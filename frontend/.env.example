# Base URL of the Django backend the frontend talks to.
# Falls back to http://127.0.0.1:8000 when not set.
# Copy this file to .env.local and adjust as needed.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
